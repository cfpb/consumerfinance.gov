/* ==========================================================================
   Settings for project environment. Used by JavaScript gulp build process.
   ========================================================================== */

'use strict';

var paths = {
  src:  './src',
  dist: './cfgov/v1/jinja2/v1',
  lib:  './src/vendor',
  test: './test'
};

module.exports = {
  paths: paths
}
