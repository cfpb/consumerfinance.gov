/* ==========================================================================
   sample: sample JS for setting up tests. This is not included in
   any pages in the site.
   ========================================================================== */

function init() {
  return 'Krang';
}

export default { init };
