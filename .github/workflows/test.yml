name: test

# Testing.
on:
  push:
    branches:
      - veracode
  pull_request:
    branches:
      - veracode

jobs:
  Veracode_SAST:
    runs-on: [ubuntu-latest]
    steps:

# Checkout code
      - uses: actions/checkout@v4.2.2
   
      - name: Setup Veracode
        run: |
         curl -fsS https://tools.veracode.com/veracode-cli/install | sh

      - name: Setup Veracode
        run: |
         ls -la
      - name: zip python and javascript files 
        run: |
          zip cfgov-gha.zip ${{ github.workspace }}/cfgov/ ${{ github.workspace }}/package.json ${{ github.workspace }}/yarn.lock ${{ github.workspace }}/.github ${{ github.workspace }}/requirements
